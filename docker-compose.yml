services:
  happ-crypto-redirect:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: happ-crypto-redirect
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
